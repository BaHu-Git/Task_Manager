name: agent-env
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - openai
      - python-dotenv
      - jinja2
      - requests
      - google-api-python-client
      - google-auth
      - google-auth-oauthlib
      - mcp
      - uvicorn

